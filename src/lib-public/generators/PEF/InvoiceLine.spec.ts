import { beforeEach, describe, it, vi } from 'vitest';
import { generateInvoiceLine } from './InvoiceLine';

describe(generateInvoiceLine.name, () => {
    beforeEach(() => {
        vi.clearAllMocks();
    });

    it('TO DO', () => {
    });
});